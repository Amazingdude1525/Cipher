import 'package:flutter/material.dart';

class QrRevealAnimation extends StatelessWidget {
  const QrRevealAnimation({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
