import 'package:cipher/core/widgets/glass_card.dart';
import 'package:flutter/material.dart';

class HeroActionCards extends StatelessWidget {
  const HeroActionCards({super.key});

  @override
  Widget build(BuildContext context) {
    return const GlassCard(child: Text('Hero Actions'));
  }
}
