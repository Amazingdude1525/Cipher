import 'package:flutter/material.dart';

class IdCardVerifierScreen extends StatelessWidget {
  const IdCardVerifierScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('ID Card Verifier')));
  }
}
